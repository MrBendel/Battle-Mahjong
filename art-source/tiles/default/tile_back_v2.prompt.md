# Default Tile Back V2 Generation Notes

Generated with OpenAI image generation using the prior terracotta back and `tile_base_v2.png` as visual references.

## Prompt

Rebuild the blank tile back so it visibly belongs to the same physical tile as the Default V2 front. Preserve a clearly distinct deep terracotta face surface, but use the front tile's compact golden-brown lower sidewall, thin dark contour, tall silhouette, lighting, and perspective. Keep the face blank for a separately composited back design. Use a transparent background with no cast shadow, text, symbol, corner scuffs, or decorative notches.

The generated master was resized to the skin's canonical `512 x 736` source geometry. Runtime export is generated at `256 x 368` by `scripts/tools/export_tile_art.gd`.
